# === Secrets / environment ===
.env
.env.*
!.env.example

# === Data (large, regenerated by scrapers/pipelines) ===
# Note: final_model.hdf5 is intentionally kept tracked (small, app needs it).
# Anchored to root so static_page/data/ (needed by the Pages site) is NOT ignored.
/data/

# === Runtime user uploads (prediction feature) ===
nba_app/static/uploads/*_temp.csv

# === Databases ===
instance/nba_oracleDB.db
*.sqlite
*.sqlite3
*.db

# === Flask runtime ===
flask_session/
instance/
flask-ui*.log
*.log

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# === Notebooks ===
.ipynb_checkpoints/

# === IDE ===
.idea/
.vscode/
